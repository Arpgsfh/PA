<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.d3ifcool.denver">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PasscodeActivity"></activity>
        <activity android:name=".TentangActivity" />
        <activity
            android:name=".ReportActivity"
            android:label="Detail Riwayat"
            android:parentActivityName=".RiwayatActivity"
            android:theme="@style/AppThemeAction">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".RiwayatActivity" />
        </activity>
        <activity
            android:name=".ProfilAnakActivity"
            android:label="Profil Anak"
            android:theme="@style/AppThemeAction" />
        <activity
            android:name=".StimulasiActivity"
            android:label="Stimulasi"
            android:theme="@style/AppThemeAction" />
        <activity android:name=".RiwayatActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PilihUmurActivity" />
        <activity
            android:name=".TestActivity"
            android:label="Skrining"
            android:theme="@style/AppThemeAction" />
        <activity android:name=".PenilaianActivity" />
        <activity android:name=".LoginActivity" />
    </application>

</manifest>